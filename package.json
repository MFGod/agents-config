{
  "name": "agents-config",
  "version": "0.5.1",
  "private": true,
  "description": "AI agent configuration kit for Claude Code and Cursor: rules, skills, lifecycle hooks.",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test"
  }
}
