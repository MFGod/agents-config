.idea/
logs/
log/
__pycache__/
.ruff_cache/
.mypy_cache/
venv/


# Секреты. Паттерны, а не конкретные пути: SECURITY.md опирается на то, что секрет
# физически не может попасть в репозиторий, и раньше это утверждение было ложным —
# закрыты были только два файла github_token, а любой .env коммитился свободно.
secrets/
.env
.env.*
*.env

# Runtime state 
.cursor/hooks/state/

# Локальное install-состояние (генерируется при установке, не для коммита)
.claude/kit-meta.json
.cursor/kit-meta.json

# Личные настройки (не дистрибутив)
.claude/settings.json

# Auto-generated plugin directories
.claude-plugin/
.claude/.claude-plugin/

# Тестовые артефакты
.test/